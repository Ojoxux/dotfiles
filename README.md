# dotfiles

```bash
git clone https://github.com/Ojoxux/dotfiles.git ~/dotfiles
cd ~/dotfiles
./setup.sh <host>
```

## daily use

```bash
task apply HOST=<host>
task apply:update HOST=<host>
task dry-run HOST=<host>
task brew:update
```
